# v2.5 Shadow Paper Observation

> 前置条件：v2.4 experiment 结论 = promote_to_shadow_paper
> 目标：同一 MinSwing entry 下同时记录 current_exit 和 fast_exit 的 shadow outcome

## 阶段 17：Shadow Paper 设计与实现

### 关键原则
```text
不重复开仓。一笔 entry，两个 exit_mode 同时 shadow 评估。
```

### 输出
```text
data/research/paper_shadow_sessions/
  entries.parquet
  current_exit_shadow.parquet
  fast_exit_shadow.parquet
  paired_shadow_comparison.parquet
  daily_summary.json
  final_report.json
```

### DB
```text
paper_sessions (session_type=exit_mode_shadow)
```

### 完成标准
```text
[ ] 至少 14 天 shadow paper 或 100 shadow entries
[ ] current_exit 与 fast_exit 可逐 entry 比较
[ ] 没有重复开仓
[ ] 所有结果写 DB
```

### 给 Claude 的任务
```text
请设计并实现 exit_mode shadow paper session。
要求：
- 同一 MinSwing entry 下同时记录 current_exit 和 fast_exit shadow outcome
- 不重复开仓
- 输出 paired_shadow_comparison.parquet
- 写 paper_sessions，session_type=exit_mode_shadow
```
