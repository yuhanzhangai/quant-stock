# v2.5 Exit Mode Framework + Shadow Paper

> 前置条件：v2.4 结论 = promote_to_shadow_paper
> 如果 v2.4 = remain_research → 继续研究，不做 v2.5
> 如果 v2.4 = reject_hybrid_exit → 停止 exit_mode 路线

## 目标

实现 exit_mode framework，但只用于 shadow paper，不改变 production 默认行为。

## 必须保证
```text
current_exit = production default
hybrid_exit = shadow candidate
FastExit = research only (已降级)
```

## 技术目标
```text
[ ] MinSwing entry 和 exit 拆分
[ ] current_exit regression lock (baseline hash)
[ ] hybrid_exit 作为 shadow mode
[ ] 同一 entry 同时评估 current_exit 和 hybrid_exit
[ ] 不重复开仓
[ ] 不影响 production paper/live
```

## 完成标准
```text
[ ] current_exit regression test 通过
[ ] shadow session 不重复开仓
[ ] shadow result 写 DB (session_type=exit_mode_shadow)
[ ] 至少 14 天 shadow observation 或 100 entries
```
