# v2.4 Hybrid Exit Candidate Validation

> 注意：v2.4 不再是 integration，而是 validation。
> 原计划 v2.4 = FastExit integration → 已废弃（FastExit 4/7 标准失败）
> 新 v2.4 = HybridExit candidate validation（v2.3R 意外发现需独立验证）

## 前置条件
- [x] v2.3R.1 compliance fix 完成
- [ ] v2.3 Closeout 完成

## 目标

正式验证 MinSwing entry + hybrid_exit 是否比 current_exit 更稳定。
不允许直接使用 v2.3R 的结果作为 promotion 依据。

## 固定规则
```text
[ ] 不改 MinSwing entry
[ ] 不调 hybrid 参数
[ ] 不新增其他 exit_mode
[ ] 不升级 production
[ ] 不把 v2.3R 的发现当成最终证明
```

## 实验假设
```yaml
hypothesis: >
  在保持 MinSwing v3 entry logic 完全不变的情况下，
  hybrid_exit 可以改善 ETH-USDT 的收益回撤特征。
  该实验源于 v2.3R 的研究线索，但本实验必须独立验证，
  不允许直接使用 v2.3R 的结果作为 promotion 依据。
```

## 必跑验证
```text
[ ] common entry paired replay (current vs hybrid)
[ ] portfolio-constrained replay
[ ] windowed replay (full/30d/60d/90d)
[ ] cost stress (normal + pessimistic)
[ ] 9-gate validation (0 ERROR required)
[ ] OOS / walk-forward
[ ] random_baseline
[ ] event_backtest
[ ] parameter_stability
```

## 判断标准
```text
[ ] ≥3/4 窗口优于 current_exit
[ ] profit factor 提升
[ ] max drawdown 不恶化
[ ] fee/gross 不恶化
[ ] 悲观成本存活
[ ] 9-gate 无 ERROR
[ ] random_baseline PASS
[ ] 不是靠少数 1-2 笔大单
```

## 结论三选一
```text
1. promote_to_shadow_paper → 进入 v2.5
2. remain_research_exit_mode → 继续研究
3. reject_hybrid_exit → 停止 exit_mode 路线
```

## 给 Claude 的任务
```text
请启动 v2.4-hybrid-exit-candidate-validation。
不是 integration，不允许修改 MinSwing production 逻辑。
验证 hybrid_exit 是否值得进入 shadow paper。
所有 artifact 必须持久化，所有结果必须写 DB。
结论只能三选一。
```
