# v2.3 优先级计划

> 来源：用户指令 2026-04-24
> 核心原则：先查 integrity，同时跑 live observation，integrity 清楚后做 replay，全部完成后才进 v2.4

## 正确顺序

1. **先查 integrity 问题**（已完成）
   - [x] backtest_runs=0 → ALTER TABLE 补 7 列，已验证写入
   - [x] random_baseline=ERROR → dtype=bool 修复，已验证 PASS

2. **同时继续 v2.3 C7 paper observation**
   - [ ] 每天记录 paper health
   - [ ] 继续积累 14 天 / 100 trades
   - 每天只做轻量检查：API/delay/session DB/signal/reject/fill

3. **integrity 问题澄清后，做 v2.3R historical replay maturity**
   - [ ] MinSwing entry + current exit
   - [ ] MinSwing entry + fast_exit
   - [ ] MinSwing entry + trailing
   - [ ] MinSwing entry + hybrid
   - 目标：测试不同 exit_mode，不是两个独立策略

4. **v2.3R + v2.3 C7 都完成后，才进入 v2.4**
   - v2.4: MinSwing exit-mode integration experiment
   - 前提条件：
     - [x] v2.3 integrity 问题清楚
     - [ ] v2.3 C7 live observation 达到 14 天或 100 trades
     - [ ] v2.3R historical replay 完成
     - [ ] FastExit 明确是 exit_variant_only（已确认：96% 重叠）

## 不要做
- 不要跳过 integrity 直接做 replay
- 不要跳过 replay 直接做 v2.4
- 不要在 v2.3 改策略参数
- 不要在 v2.3 升级 FastExit 到 Production

## 每日 live observation 检查清单
- [ ] API 正常
- [ ] latest_bar_delay 正常
- [ ] session 正在写 DB
- [ ] signal/reject/fill 正常记录
- [ ] 没有重复信号
- [ ] 没有缺失 exit_reason
