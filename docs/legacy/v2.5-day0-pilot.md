# v2.5 True OOS Shadow Paper — Day 0 / Day 1 Pilot

> 位于 v2.4.1 和 v2.5 完整观察之间
> 目的：启动 shadow pipeline，验证基础设施可用，不做任何策略结论
> 这不是 v2.5 完成，只是 v2.5 的开始

## 今晚只做
```text
[ ] API health PASS
[ ] latest_bar_delay PASS
[ ] entry_ts > v2.4 completed_at
[ ] overlap_with_v2_4_entries = 0
[ ] overlap_with_v2_3R_entries = 0
[ ] current_exit_shadow 正常记录
[ ] hybrid_exit_shadow 正常记录
[ ] paired_shadow_comparison 正常生成
[ ] 不重复开仓
[ ] 不修改参数
[ ] paper_sessions 写 DB
```

## 今晚不做
```text
不启动 v2.6
不做 evidence review
不修改策略参数
不让 hybrid_exit 变成真实执行
不因为没有 entry 而放宽规则
不得出 hybrid 有效/无效的结论
```

## 今晚只能得出的结论
```text
shadow pipeline works / doesn't work
API/data freshness OK / not OK
new entries are OOS / not OOS
DB/artifacts written correctly / incorrectly
```

## v2.5 完成仍需要
```text
30 天 shadow observation 或 100 个全新 entries
AND differential_exit_count >= 30
AND overlap = 0
AND concentration gate
AND drawdown gate
AND fee/gross gate
```

## 输出
```text
reports/v2_5_true_oos_shadow/daily/YYYYMMDD_shadow_health.md
data/research/paper_shadow_sessions/session_id=.../
  entries.parquet
  current_exit_shadow.parquet
  hybrid_exit_shadow.parquet
  paired_shadow_comparison.parquet
  daily_summary.json
  oos_independence_check.json
DB: paper_sessions (session_type=exit_mode_shadow)
```
