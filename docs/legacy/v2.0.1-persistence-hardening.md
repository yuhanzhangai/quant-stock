要修，而且要先修这个，再做 v2.1 acceptance。
你这个审计很关键。它说明 v2.0 的架构已经搭起来了，但还有一些 v1.1 的习惯残留：文件系统仍然在部分地方充当“事实来源”，DB 写入失败还会被静默吞掉。既然你现在的目标是让 research.duckdb 成为研究数据的单一真相源，而它已经承担了 backtest_runs、validation_results、paper_sessions、data_manifest、data_quality_checks 等核心表，那么这些问题必须修。
我建议把这次修复单独定为：
v2.0.1-persistence-hardening
不要把它混进策略优化，也不要顺手新增功能。目标只有一个：
所有正式研究结果必须可靠写入 DB；写不进去就失败；Dashboard 研究页面必须以 DB 为索引。

结论：四个问题的处理优先级
问题严重级别是否现在修原因DB 不存在静默跳过P0必须马上修破坏 research.duckdb 单一真相源validate DB 写入 fire-and-forgetP0必须马上修validation pass/fail 可能只存在 JSON，不进 DB参数搜索和最优结果不写 DBP1必须修会导致最关键的策略选择过程不可追踪PaperSession 依赖手动 save_allP1必须修paper calibration 阶段会产生孤儿 sessionDashboard 4/10 扫文件P2应该修研究结果页面应该从 DB 列表进入 artifactDashboard 1/2/3/5 扫 OHLCV 文件P3暂时可以不修它们是市场数据页面，不是研究真相页面
重点是：页面 1/2/3/5 直接读 K 线文件可以暂时接受；页面 4/10 不应该通过 glob 找研究结果。
你现在 v2.0 已经把回测输出标准化成 config.yml + metrics.json + trades.parquet + equity.parquet，并且把研究数据库作为结果索引；如果 Dashboard 还靠扫 HTML 或扫 paper session 文件夹，就会重新退回 v1.1 的“文件夹即数据库”模式。

v2.0.1 修复范围
Fix 1：所有正式 DB 写入必须 fail-fast
现在这种逻辑要废掉：
if not DB_PATH.exists():    return
这行是最大问题。
应该改成：
if not DB_PATH.exists():    raise RuntimeError(        f"research database not found: {DB_PATH}. "        "Run: python scripts/init_research_db.py --seed"    )
更好的做法是统一封装一个 DB guard。
建议新增：
src/research/  __init__.py  db.py
示例：
from pathlib import Pathimport duckdbDB_PATH = Path("data/meta/research.duckdb")class ResearchDBUnavailable(RuntimeError):    passdef connect_research_db(required: bool = True) -> duckdb.DuckDBPyConnection | None:    if not DB_PATH.exists():        if required:            raise ResearchDBUnavailable(                f"research.duckdb not found at {DB_PATH}. "                "Run `python scripts/init_research_db.py --seed` before research runs."            )        return None    return duckdb.connect(str(DB_PATH))
然后所有这些地方都改成用它：
src/backtest/standardized_output.pysrc/validation/report.pysrc/backtest/paper_session.pyscripts/run_backtest.pyscripts/validate_strategy.py
通过标准：
[ ] rg "if not DB_PATH.exists\(\): return" src scripts 返回 0[ ] DB 不存在时 run_backtest.py 直接失败[ ] DB 不存在时 validate_strategy.py 直接失败[ ] DB 不存在时 paper session finalize 直接失败

Fix 2：DB 写入失败不能只写 JSON 后继续成功
你审计里说 validate_strategy.py 会无条件保存 JSON，但 _save_to_db() 失败不报错。这个要改。
原因很简单：
如果 JSON 有 validation_report，但 DB 没有 validation_results，Dashboard 和策略门禁就会认为它没有被验证。
这会让系统出现两个真相源：
文件说 passDB 里不存在Dashboard 看不到策略状态无法判断
修法：
def save_validation_report(report: ValidationReport) -> None:    save_json(report)    save_to_db(report)  # 必须失败就 raise
如果你担心调试时不想写 DB，可以加一个显式 scratch 模式，但默认必须 strict：
python scripts/validate_strategy.py --strategy minswing_v3 --symbol ETH-USDT
正式模式：DB 必须成功。
python scripts/validate_strategy.py --strategy minswing_v3 --symbol ETH-USDT --scratch
scratch 模式：可以只写文件，但不能进入策略决策。
我建议短期先不要加 --scratch，减少分支逻辑。
通过标准：
[ ] validation_report.json 保存成功但 DB 写失败时，命令 exit non-zero[ ] validation_results 表中能查到 9 个 gate 的记录[ ] Dashboard 策略门禁页面显示结果和 JSON 一致

Fix 3：run_backtest.py 的所有决策型结果都必须进 backtest_runs
你审计发现：
第一次单次回测写 DB参数搜索 9 个组合没写 DB最优参数第二次回测也没写 DB
这个必须修。
不过我不建议你给每个参数组合都保存完整 artifact 目录，因为以后参数搜索可能从 9 个变成 900 个，目录会爆炸。
建议分两层：
参数组合：  写 backtest_runs 指标行  run_type = grid_candidate  不一定保存完整 trades/equity最终最优：  写 backtest_runs 指标行  run_type = grid_best  保存完整 config / metrics / trades / equity普通单次回测：  写 backtest_runs 指标行  run_type = single  保存完整 artifact
你可以给 backtest_runs 加几个轻量字段：
ALTER TABLE backtest_runs ADD COLUMN run_type TEXT DEFAULT 'single';ALTER TABLE backtest_runs ADD COLUMN parent_run_id TEXT;ALTER TABLE backtest_runs ADD COLUMN output_dir TEXT;ALTER TABLE backtest_runs ADD COLUMN params_hash TEXT;ALTER TABLE backtest_runs ADD COLUMN data_version TEXT;ALTER TABLE backtest_runs ADD COLUMN code_commit TEXT;ALTER TABLE backtest_runs ADD COLUMN is_decision_run BOOLEAN DEFAULT true;
不要新增表，先加字段就够了。
推荐 run_type：
singlebaselinegrid_candidategrid_bestooswalk_forward_windowcost_stress_optimisticcost_stress_normalcost_stress_pessimisticpaper_reference
参数搜索时：
parent_run_id = 本次搜索的总 run_idgrid_candidate = 每个参数组合grid_best = 被选中的最终参数
通过标准：
[ ] 单次回测后 backtest_runs 增加 1 行[ ] 9 组合参数搜索后至少增加 9 行 grid_candidate[ ] 最优参数复跑后增加 1 行 grid_best[ ] grid_best 有 output_dir[ ] grid_best 的 metrics.json 与 backtest_runs 指标一致
验证 SQL：
SELECT run_type, COUNT(*)FROM backtest_runsWHERE parent_run_id = '你的参数搜索run_id'GROUP BY run_type;
期望看到类似：
grid_candidate | 9grid_best      | 1

Fix 4：save_all(write_db=False) 这种逃逸口要收紧
你提到：
write_db=True 是默认值，但调用者可以传 False 跳过
我建议改成：
正式保存路径不允许 write_db=False
也就是说：
save_all()
内部强制：
save_files()save_to_db(required=True)
不要让调用者随手绕过 DB。
如果以后确实要 scratch run，应该另开清晰 API：
save_scratch()
并且输出路径放到：
data/research/scratch/
这样 scratch 结果不会混进正式研究结果。
不要这样：
save_all(write_db=False)
因为这个参数很危险。它看起来只是“少写一点”，实际是在破坏单一真相源。
通过标准：
[ ] save_all 不再暴露 write_db 参数[ ] 正式 run 必须写 DB[ ] scratch run 不进入 backtest_runs / validation_results / paper_sessions

Fix 5：PaperSession 必须自动 finalize，不依赖使用者记得 save_all
你审计说 PaperSession 的使用者必须手动调 save_all()，这个在 paper trading 阶段风险很大。
建议改成上下文管理器：
with PaperSession(config) as session:    session.record_signal(...)    session.record_fill(...)    session.record_trade(...)
退出时自动：
session.finalize()
finalize() 内部：
save_files()save_to_db(required=True)mark_status_completed()
如果中途异常，也要保存错误状态：
mark_status_failed(error=str(exc))save_partial_files()save_to_db(required=True)
Paper session 状态建议：
createdrunningcompletedfailedaborted
如果 paper_sessions 表现在没有这些字段，可以加：
ALTER TABLE paper_sessions ADD COLUMN status TEXT DEFAULT 'created';ALTER TABLE paper_sessions ADD COLUMN output_dir TEXT;ALTER TABLE paper_sessions ADD COLUMN started_at TIMESTAMP;ALTER TABLE paper_sessions ADD COLUMN ended_at TIMESTAMP;ALTER TABLE paper_sessions ADD COLUMN linked_backtest_run_id TEXT;ALTER TABLE paper_sessions ADD COLUMN error_message TEXT;
通过标准：
[ ] paper_trader.py 正常退出时一定有 paper_sessions 记录[ ] paper_trader.py 异常退出时也有 failed session 记录[ ] final_report.json 和 paper_sessions 表一致[ ] 不调用 save_all 也不会丢 session

Fix 6：Dashboard 页面 4 改成 DB 索引，不再扫 HTML
页面 4 现在：
glob("*.html") 扫 reports 目录
这个要改。
新的逻辑应该是：
SELECT    backtest_id,    run_id,    strategy_name,    symbol,    timeframe,    start_ts,    end_ts,    net_return,    sharpe,    profit_factor,    max_drawdown,    trade_count,    run_type,    output_dir,    created_atFROM backtest_runsORDER BY created_at DESC;
用户选中某个 run 后，再从 output_dir 读取：
metrics.jsontrades.parquetequity.parquethtml_report.html
注意：从 DB 选中后读取 artifact 文件是可以的。问题不是读文件，问题是用文件系统 glob 当索引。
页面 4 正确模式是：
DB 负责列出有哪些回测artifact 文件负责承载大对象
通过标准：
[ ] 页面 4 不再 glob reports/*.html[ ] 页面 4 的回测列表来自 backtest_runs[ ] 页面 4 选中 run_id 后再读 output_dir[ ] 如果 artifact 缺失，页面显示 warning，而不是直接消失

Fix 7：Dashboard 页面 10 改成 DB 索引，不再扫 paper_sessions/backtests
页面 10 现在扫：
paper_sessions 目录backtests 目录
应该改成：
SELECT    session_id,    strategy_name,    strategy_version,    symbol,    timeframe,    status,    started_at,    ended_at,    linked_backtest_run_id,    output_dirFROM paper_sessionsORDER BY started_at DESC;
如果有 linked_backtest_run_id，再查：
SELECT *FROM backtest_runsWHERE run_id = ?;
然后页面再读取：
paper output_dir/signals.parquetpaper output_dir/accepted_trades.parquetpaper output_dir/rejected_signals.parquetpaper output_dir/fills.parquetpaper output_dir/equity.parquetpaper output_dir/final_report.jsonpaper output_dir/paper_vs_backtest.json
通过标准：
[ ] 页面 10 session 列表来自 paper_sessions[ ] 页面 10 backtest 对照来自 backtest_runs[ ] 页面 10 不再 glob paper_sessions/backtests[ ] 缺失 artifact 时显示 DB 有记录但文件缺失
这点很重要。因为以后你要区分：
DB 没有记录 = 没发生过DB 有记录但 artifact 缺失 = 存储损坏artifact 有文件但 DB 没记录 = 非正式 / 孤儿结果

页面 1/2/3/5 暂时不用急着修
你审计说页面 1/2/3/5 在扫 OHLCV 或 funding 目录。这个可以暂时接受，因为：
K线原始数据本来不在 research.duckdb 里research.duckdb 只是研究元数据库data_manifest 只记录文件元信息，不存完整K线
所以短期内：
页面 1 市场总览：可以读 parquet页面 2 资金费监控：可以读 funding parquet页面 3 因子表现：可以读 OHLCV/factor parquet页面 5 策略回测：可以读 OHLCV parquet
但有一个小优化可以做，不是必须：
symbol/timeframe 列表从 data_manifest 读；具体K线内容再读 parquet。
也就是说，页面不要用 iterdir() 找有哪些币，而是：
SELECT DISTINCT symbol, timeframeFROM data_manifestWHERE dataset = 'ohlcv'ORDER BY symbol, timeframe;
这是 P3，不要现在抢 P0/P1 的时间。

v2.0.1 实施顺序
我建议按这个顺序修：
1. 建 src/research/db.py，统一 DB 连接和 fail-fast2. 删除所有 if not DB_PATH.exists(): return3. validate_strategy.py DB 写入失败直接失败4. standardized_output.save_all 强制写 DB5. run_backtest.py 参数搜索和最优结果都写 backtest_runs6. PaperSession 改 finalize / context manager7. Dashboard 页面 4 改 DB 索引8. Dashboard 页面 10 改 DB 索引9. 加测试10. 跑 acceptance
不要先改 Dashboard。
先把 DB 写入语义修硬，再改读取端。

建议新增测试
Test 1：DB 不存在时回测失败
Given research.duckdb 不存在When run_backtest.py 保存正式结果Then 抛出 ResearchDBUnavailableAnd 不产生“看似成功”的正式 run
Test 2：validation DB 写入失败时命令失败
Given validation_report.json 可以写And research.duckdb 不可写When validate_strategy.py 运行Then 命令失败And 不允许显示 validation completed
Test 3：参数搜索写入 DB
Given 3x3 参数搜索When run_backtest.py 执行Then backtest_runs 至少包含 9 个 grid_candidateAnd 包含 1 个 grid_best
Test 4：PaperSession 自动 finalize
Given with PaperSession(...) as sessionWhen context 正常退出Then paper_sessions 有 completed 记录And output_dir 存在 final_report.json
Test 5：PaperSession 异常也记录 failed
Given with PaperSession(...) as sessionWhen 内部抛异常Then paper_sessions 有 failed 记录And error_message 不为空
Test 6：Dashboard 4/10 不再 glob
用简单 grep 做 CI 检查也可以：
rg "glob\\(|iterdir\\(" dashboard/pages/4_回测查看.py dashboard/pages/10_Paper验证.py
通过标准：
无输出

v2.0.1 Acceptance Checklist
你可以直接建：
docs/V2_0_1_PERSISTENCE_HARDENING.md
内容：
# v2.0.1 Persistence Hardening Checklist## DB Strictness[ ] src/research/db.py exists[ ] all formal DB writes use connect_research_db(required=True)[ ] no silent `if not DB_PATH.exists(): return`[ ] DB write failure raises error[ ] validate_strategy fails if validation_results cannot be written[ ] run_backtest fails if backtest_runs cannot be written[ ] paper session fails or marks failed if paper_sessions cannot be written## Backtest Persistence[ ] single backtest writes backtest_runs[ ] parameter search writes grid_candidate rows[ ] selected best run writes grid_best row[ ] grid_best has output_dir[ ] metrics.json matches backtest_runs row## Paper Persistence[ ] PaperSession supports context manager or guaranteed finalize[ ] completed session writes paper_sessions[ ] failed session writes paper_sessions with status=failed[ ] output_dir is stored in DB[ ] linked_backtest_run_id is stored when available## Dashboard[ ] page 4 lists backtests from backtest_runs[ ] page 4 reads artifacts only after selecting DB row[ ] page 10 lists sessions from paper_sessions[ ] page 10 links to backtest_runs through linked_backtest_run_id[ ] page 4/10 no longer glob directories as source of truth## Tests[ ] missing DB raises error[ ] validation DB failure fails command[ ] grid search persistence test passes[ ] PaperSession finalize test passes[ ] Dashboard grep check passes## Release[ ] pytest passes[ ] ruff passes[ ] docs updated[ ] git tag: v2.0.1-persistence-hardening

修完后的判断标准
修完以后，你应该能回答这几个问题：
1. 任何一个正式 backtest，DB 里一定有记录吗？2. 任何一个 validation，DB 里一定有 9 gate 结果吗？3. 任何一个 paper session，DB 里一定有 session 状态吗？4. Dashboard 看到的研究结果，是否全部来自 DB？5. 文件夹里多出来的孤儿 artifact，是否不会被 Dashboard 当成正式结果？
这五个答案必须都是：
是

最后建议
你的判断是对的：这不是大重构，而是一次持久化语义修硬。
现在不要继续做 v2.1 的策略验收，也不要跑 paper calibration。因为 v2.1 的所有结论都依赖 DB 记录可信。如果现在 DB 写入还能静默跳过，那 v2.1 跑出来的验收结果也不够硬。
当前正确顺序是：
v2.0.1-persistence-hardening  ↓v2.1-validation-hardening  ↓paper calibration  ↓策略状态更新
你已经把平台从 v1.1 的文件报告时代升级到了 v2.0 的研究数据库时代；这次修复就是把最后几处“文件夹即数据库”的残留清掉。