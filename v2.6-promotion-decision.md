# v2.6 Hybrid Exit Evidence Review

> 改名：从 "Promotion Review" 改为 "Evidence Review"
> 前置条件：v2.5 结论 = promote_to_formal_exit_mode_experiment
> 如果 v2.5 = continue_shadow → 不进入 v2.6
> 如果 v2.5 = remain_research / reject → v2.6 只做 closeout

## 目标

汇总 v2.3R → v2.4 → v2.5 全部证据，评估 hybrid_exit 最终状态。
首要目标不是 promotion，而是证据强度评估。

## 要汇总的证据
```text
v2.3R: historical replay lead (同一数据)
v2.4: conditional formal replay (不独立)
v2.5: true OOS shadow paper (独立)

concentration: v2.4 top5=78.8%, v2.5 top5=?
differential: v2.4 12/84, v2.5 ?/? 
drawdown: v2.4 30d hybrid worse, v2.5 ?
parameter_stability: v2.4 FAIL 0.40, v2.5 ?
```

## 决策矩阵
```text
1. keep_current_exit_default
   v2.5 不支持 hybrid / concentration 仍高 / differential 不足

2. keep_hybrid_as_research
   有一些优势但不稳定

3. keep_hybrid_as_shadow_candidate
   v2.5 支持但样本仍不足

4. add_hybrid_as_optional_exit_mode_disabled_by_default
   v2.5 OOS 支持 + concentration 通过 + drawdown/fee 通过

5. promote_hybrid_to_ETH_default
   极严格条件（见下）
```

## promote_hybrid_to_ETH_default 条件（全部满足）
```text
[ ] v2.5 true OOS passed
[ ] differential_exit_count >= 50
[ ] concentration_gate PASS (top5 <= 60%)
[ ] drawdown_gate PASS
[ ] fee_gross_gate PASS
[ ] hybrid net edge positive and stable
[ ] at least 2 independent OOS windows favorable
[ ] 9-gate no ERROR
[ ] random_baseline PASS
[ ] parameter_stability no longer FAIL or justified waiver
[ ] current_exit regression remains intact
```

如果 parameter_stability 继续 FAIL：
```text
不能 default → 最多 optional disabled_by_default
```
