# v2.6 Promotion Decision

> 前置条件：v2.3R + v2.4 + v2.5 全部完成
> 目标：决定 fast_exit exit_mode 的最终状态

## 阶段 18：Exit-Mode Promotion Review

### 可能结论
```text
1. keep_current_exit_as_default — 继续使用 current_exit，fast_exit 保留研究状态
2. add_fast_exit_as_optional_candidate — fast_exit 作为可选 exit_mode，但不默认启用
3. promote_fast_exit_to_default_for_ETH — 仅 ETH 默认使用 fast_exit
4. reject_fast_exit — 删除或归档 fast_exit exit_mode
```

### 最严格条件（promote 需要全部满足）
```text
[ ] v2.3R 支持
[ ] v2.4 9-gate 支持
[ ] v2.5 shadow paper 支持
[ ] 多窗口稳定
[ ] 成本压力下存活
[ ] 不恶化 drawdown
[ ] 不明显牺牲大趋势
[ ] 不是靠少数大单
[ ] live shadow 结果支持历史结论
```

### 给 Claude 的任务
```text
请根据 v2.3R、v2.4、v2.5 的所有结果生成 v2.6 exit_mode promotion review。
不要新增测试。不要改策略。
只做决策报告和 registry metadata 更新。
```
