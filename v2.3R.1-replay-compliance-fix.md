# v2.3R.1 Replay Compliance Fix

> v2.3R 分析结论合理，但 Phase 5-8 违反平台规范。
> 本阶段只修复合规问题，不改变策略结论。

## 当前状态

```text
v2.3R 分析结论：
  FastExit: remain_research_exit_mode (4/7 标准失败)
  HybridExit: promising_research_lead (意外发现)
  MinSwing v3: remain_production

v2.3R 合规问题：
  Phase 5: portfolio replay 没有 artifact，没写 backtest_runs
  Phase 6: windowed replay 没有 summary 文件
  Phase 7: cost stress 没有 summary 文件
  Phase 8: 9-gate 验证对象和 replay 对象不一致
  Phase 4: 缺少自动化测试
  exit_modes.py: current_exit trail=True 硬编码
```

## A1. current_exit 参数硬编码修复

```text
[ ] current_exit 从 config/COIN_CONFIGS 读取 trail 参数
[ ] ETH replay 结果不变（regression test）
```

## A2. Phase 4 自动化测试补齐

```text
[ ] test_all_exit_modes_use_same_entries
[ ] test_paired_comparison_has_one_row_per_entry
[ ] test_fast_exit_not_independent_entry_logic
[ ] test_exit_mode_outputs_required_columns
```

## A3. Phase 5 portfolio replay 持久化

```text
每个 exit_mode 输出:
  data/research/replay/run_id=.../portfolio/exit_mode=xxx/
    trades.parquet, equity.parquet, metrics.json, rejected_entries.parquet

写入 backtest_runs:
  run_type = exit_mode_portfolio_replay
  parent_run_id, strategy_name, symbol, output_dir, params_hash
```

## A4. Phase 6 windowed replay 持久化

```text
用 timestamp window（不用 bar index 估算）
输出: windowed_summary.csv + windowed_summary.json
```

## A5. Phase 7 cost stress 持久化

```text
输出: cost_stress_summary.csv + cost_stress_summary.json
字段: exit_mode, cost_scenario, trade_count, net_return, profit_factor,
      max_drawdown, fee_total, fee_to_gross_pnl, survives_cost_stress
```

## A6. Phase 8 9-gate 验证对象修复

```text
验证对象 = MinSwing entry + exit_mode (不是原始策略函数)
至少验证: current_exit, fast_exit, hybrid_exit
输出: validation_exit_modes.md, 写入 validation_results
```

## A7. 更新决策报告

```text
reports/v2_3_replay_maturity/exit_mode_replay_maturity.md 加 Compliance Fix Result
FastExit: remain_research_exit_mode (不变)
HybridExit: promising_research_lead (不允许写 ready for integration)
```

## A8. 完成标准

```text
[ ] Phase 4 tests added
[ ] Phase 5 artifacts + backtest_runs
[ ] Phase 6 windowed summaries
[ ] Phase 7 cost stress summaries
[ ] Phase 8 validation object aligned
[ ] Report updated
[ ] pytest pass
[ ] ruff pass
[ ] A1+A2 双重复核
```

## 给 Claude 的任务

```text
请执行 v2.3R.1 Replay Compliance Fix。
目标不是改变策略结论，而是修复 v2.3R 中违反平台规范的问题。
必须完成 A1-A7 所有项。FastExit 保持 remain_research_exit_mode。
HybridExit 只标记 promising_research_lead。
所有 artifact 必须写文件 + 写 DB。
```
