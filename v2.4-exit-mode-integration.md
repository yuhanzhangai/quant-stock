# v2.4 MinSwing Exit-Mode Integration [SUPERSEDED]

> **此文件已废弃。** v2.3R 证明 FastExit 4/7 标准失败，不进入 integration。
> 新的 v2.4 spec 见：`v2.4-hybrid-exit-candidate-validation.md`
>
> 原前置条件：v2.3R decision = convert_to_exit_mode_candidate
> 目标：把 MinSwing 出场逻辑模块化，加入 exit_mode 框架

## 阶段 12：开始条件检查 (v2.4-readiness-check)

### 必须全部满足
```text
[ ] v2.3R decision = convert_to_exit_mode_candidate
[ ] v2.3 final report 允许启动 v2.4
[ ] current_exit 可复现
[ ] fast_exit 有明确 replay 支持
[ ] 9-gate 无 ERROR
[ ] data_quality 无未解释 warning
[ ] live paper observation 无严重异常
```

### 给 Claude 的任务
```text
请生成 V2_4_READINESS_CHECK.md。
只根据已有 v2.3R 和 v2.3 final report 判断是否允许启动 v2.4。
不要写新代码。
```

---

## 阶段 13：exit_mode 设计 (v2.4-exit-mode-design)

从 `minswing_v3 = entry + fixed exit` 改为 `minswing_v3 = entry_mode + exit_mode`。
第一步只写设计文档，不改代码。

### 目标配置
```yaml
strategy_name: minswing_v3
strategy_version: 2.4.0
entry_mode: standard
exit_mode: current_exit
available_exit_modes:
  current_exit: { status: production_default }
  fast_exit: { status: candidate }
  trailing_exit: { status: research }
  hybrid_exit: { status: research }
```

### 给 Claude 的任务
```text
请创建 v2.4 MinSwing exit_mode integration 设计文档。
只写文档，不改代码。
重点说明如何保持 current_exit 完全兼容，以及 fast_exit 如何作为 candidate exit_mode 接入。
```

---

## 阶段 14：current_exit 回归保护 (v2.4-current-exit-regression-lock)

在重构前锁定当前行为。

### 给 Claude 的任务
```text
请为 MinSwing v3 current_exit 创建重构前 regression baseline。
保存 trades/equity/metrics/hash。
添加测试，确保未来 exit_mode=current_exit 输出和 baseline 一致。
不要重构策略。
```

---

## 阶段 15：exit_mode 接口实现 (v2.4-exit-mode-interface)

把出场逻辑模块化，默认行为不变。

### 给 Claude 的任务
```text
请实现 MinSwing exit_mode 接口。
要求：
- 默认 current_exit 行为必须与 baseline 完全一致
- 增加 fast_exit 作为 candidate exit_mode
- trades 输出增加 exit_mode 字段
- 不改变 entry logic
- 不改变 production 默认配置
- regression test 必须通过
```

---

## 阶段 16：exit_mode experiment (v2.4-exit-mode-experiment)

把 fast_exit 作为 MinSwing exit_mode 跑完整 9-gate 实验。

### 结论
```text
promote_to_shadow_paper / remain_candidate_exit_mode / reject_exit_mode
```

### 给 Claude 的任务
```text
请创建并运行 v2.4 MinSwing fast_exit exit_mode experiment。
要求：
- 保持 entry logic 不变
- 比较 current_exit vs fast_exit
- 跑完整 9-gate validation
- 输出 fast_exit_mode_experiment.md
- 结论只能是 promote_to_shadow_paper / remain_candidate_exit_mode / reject_exit_mode
```
