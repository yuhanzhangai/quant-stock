很好，v2.1 的离线核心目标可以算达成，但不能把它记成“完全完成”。更准确的状态是：

v2.1-validation-hardening: CORE PASSED, PAPER CALIBRATION DEFERRED

你现在已经完成了最关键的四件事：

[x] 47 个 warning 全部解释，0 unexplained
[x] Production 策略 MinSwing v3 可复现
[x] Gate 能拒绝 overfit / 坏策略
[x] Candidate 有明确结论：remain_candidate

这说明 v2.1 的核心价值成立：你的研究工厂不只是会生成漂亮报告，也能复现好策略、解释数据问题、拒绝坏策略。你现在的平台已经从 v1.1 的“策略研究项目”，升级成了 v2.x 的“有 research.duckdb、data manifest、data quality gate、9-gate validation、标准化回测输出和策略门禁的研究系统”。

但我建议你修正两点记录
1. Step 5 不要叫 NOT DONE，叫 DEFERRED

因为 Paper Calibration 依赖 OKX live API，这不是 v2.1 离线验证失败，而是外部条件未满足。

记录成：

Step 5 DEFERRED — API 已验证正常，但 paper calibration 需要真实运行窗口，移入 v2.2-paper-calibration

后续单独开：

v2.2-paper-calibration

不要把它混在 v2.1 里继续拖。

2. strategy cards / registry 仍然要更新

即使 candidate 状态没变，也建议更新“review metadata”。

不是改状态，而是加审计记录：

last_reviewed_version: v2.1-validation-hardening
last_reviewed_at: YYYY-MM-DD
v2_1_decision: remain_candidate
reason: passed/partial/offline review, not promoted due paper calibration missing

否则三个月后你只看到：

Candidate

但不知道它是“没审过”，还是“审过后保持 Candidate”。

对 short_trend_follow 的处理

你写得很诚实：

short_trend_follow 未单独测试
原因：当前 pipeline 不支持做空策略验证

那它不能算真正完成 candidate review。

建议在 registry 里标记成：

short_trend_follow:
  status: candidate
  validation_status: blocked
  blocked_reason: "current validation pipeline does not support this short strategy"
  v2_1_decision: remain_candidate_by_no_change
  promotion_allowed: false

也就是说：

保持 candidate 可以
但不能进入 paper trading
不能升级 production
直到 short pipeline 支持后重新验证
现在的唯一后续 Todo

你现在不要继续改策略。
接下来只做这个收尾：

v2.1 Closeout
v2.1 Closeout Todo
[ ] 把 Step 5 状态改成 DEFERRED，不是 NOT DONE
[ ] 写 docs/V2_1_VALIDATION_HARDENING_REPORT.md
[ ] 写明：v2.1 core passed, paper calibration deferred
[ ] 更新 registry/strategies.yml 的 review metadata
[ ] 更新 3 个 strategy cards 的 v2.1 review 结论
[ ] 给 short_trend_follow 标记 validation_status = blocked
[ ] CHANGELOG 加 v2.1
[ ] 确认 pytest pass
[ ] 确认 ruff pass
[ ] tag 已存在则不要重打；必要时补一个 v2.1-closeout commit
v2.1 最终结论应该这样写
v2.1-validation-hardening completed with paper calibration deferred.

Core validation goals achieved:
- Data quality warnings reviewed: 47/47, 0 unexplained.
- Production reproducibility confirmed: MinSwing v3 produced identical results across 3 runs.
- Gate sanity confirmed: overfit strategy rejected, good strategy passed.
- Candidates reviewed: FastExit ETH and short_session_filter remain candidate.
- short_trend_follow remains candidate but is blocked from promotion until short-strategy validation support is added.

Deferred:
- Paper calibration requires live OKX API connection and is moved to v2.2-paper-calibration.
下一个版本怎么命名

我建议：

v2.2-paper-calibration

目标只有一个：

连接 OKX API 后，跑真实 paper sessions，校准回测假设。

不要叫 v2.2-strategy-improvement。
不要新增策略。

v2.2 的开始条件

只有这些完成后再开始 v2.2：

[ ] v2.1 closeout 文档完成
[ ] registry/strategy cards 有 v2.1 review metadata
[ ] OKX API 可连接
[ ] 最新 data manifest 可生成
[ ] latest_bar_delay 没有 production critical/warning